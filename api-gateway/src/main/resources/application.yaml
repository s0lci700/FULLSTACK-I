server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
#      routes:
#        - id: ms-<id>
#          uri: lb://ms-<id>
#          predicates:
#            - Path=/api/<id>/**
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/